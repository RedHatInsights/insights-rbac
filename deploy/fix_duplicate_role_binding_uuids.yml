apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: fix-duplicate-role-binding-uuids
objects:
  - apiVersion: cloud.redhat.com/v1alpha1
    kind: ClowdJobInvocation
    metadata:
      labels:
        app: rbac
      name: fix-duplicate-role-binding-uuids-${RUN_NUMBER}
    spec:
      appName: rbac
      jobs:
        - fix-duplicate-role-binding-uuids
parameters:
  - name: RUN_NUMBER
    description: Used to track and re-run the job
    value: '1'

